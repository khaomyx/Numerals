<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Octagon shape background -->
    <item>
        <rotate
            android:fromDegrees="45"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape android:shape="rectangle">
                <solid android:color="@color/mustard" />
            </shape>
        </rotate>
    </item>

    <!-- Outer border for the octagon -->
    <item>
        <rotate
            android:fromDegrees="45"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape android:shape="rectangle">
                <stroke
                    android:color="@color/mustard"
                    android:width="2dp" />
            </shape>
        </rotate>
    </item>

</layer-list>
